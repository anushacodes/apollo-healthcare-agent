# TODO: Step 2 — JWT auth
# POST /api/auth/login — returns {access_token, token_type}
# get_current_user dependency — decodes Bearer token
# `python -m app.auth init` — bcrypt-hashes the password and prints it
