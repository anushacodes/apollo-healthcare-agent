# TODO: Step 5 — Document upload and status
# POST /api/patients/{id}/documents   — multipart upload → 202 + doc_id
# GET  /api/patients/{id}/documents   — list documents with status
# GET  /api/documents/{id}/status     — SSE stream until done/error
