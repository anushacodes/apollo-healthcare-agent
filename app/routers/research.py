# TODO: Step 9 — Research agent status + papers
# GET /api/patients/{id}/research   — agent run status + list of research docs
# POST /api/patients/{id}/research/run  — manual trigger (DEV_MODE only)
